
# def exec_store_proc(name, params, is_col_required=False):
#     try:
#         conn = pyodbc.connect(connection_string)
#         cursor = conn.cursor()
#         if params:
#             param_string = ','.join(['?'] * len(params))
#         else:
#             param_string = ''
#         if params:
#             cursor.execute("EXEC [dbo]." + name + param_string, params)
#         else:
#             cursor.execute(f"EXEC [dbo]. {name}")
#         results = cursor.fetchone()
#         # if is_col_required:
#         #     columns = [column[0] for column in cursor.description]
#         #     results = pd.DataFrame.from_records(results, columns=columns)
#         #     return results
#     except Exception as e:
#         print(str(e))
